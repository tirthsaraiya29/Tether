package com.tether.phone.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Tether Liquid Glass Design System - Refined Color Tokens
 * Inspired by refractive optics, deep space, and high-precision instrumentation.
 */

// --- Foundation: Deep Space Atmosphere ---
val DeepSpace = Color(0xFF020205)      // The absolute abyss
val Obsidian = Color(0xFF08080C)       // Base UI surface
val SurfaceVeneer = Color(0xFF0E0E16)  // Elevated surface
val SurfaceElevated = Color(0xFF141420) // Highest elevation

// --- Primary Accent: Liquid Cyan (High Precision) ---
val LiquidCyan = Color(0xFF00E5FF)
val LiquidCyanMuted = Color(0xFF0091A1)

// --- Success Accent: Integrity Green ---
val IntegrityGreen = Color(0xFF00FF9D)

// --- Danger Accent: Alert Red ---
val AlertRed = Color(0xFFFF3366)
val AlertRedMuted = Color(0xFFB32447)

// --- Warning Accent: Matrix Gold ---
val MatrixGold = Color(0xFFFFB300)

// --- Glass & Refractive Material Properties ---
val GlassBorder = Color(0x1AFFFFFF)     // Subtle white border
val GlassBorderBright = Color(0x40FFFFFF) // For highlights

// --- Typographic Surfaces (Optimized for Contrast) ---
val TextPrimary = Color(0xFFF8F8FF)     // Ghost White
val TextSecondary = Color(0xFFA0A0B8)   // Cool Grey
val TextMuted = Color(0xFF6A6A85)       // Deep Grey
