<Window x:Class="Tether.DesktopUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewModels="clr-namespace:Tether.DesktopUI.ViewModels"
        Title="Tether Desktop Dashboard" 
        Height="680" Width="960" 
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <Border Grid.Column="0" Background="{DynamicResource MaterialDesignPrimaryBackground}">
            <StackPanel Margin="10">
                <TextBlock Text="TETHER" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource MaterialDesignPrimaryForeground}" Margin="0,0,0,30"/>
                <ListBox ItemsSource="{Binding NavItems}" SelectedItem="{Binding SelectedNavItem}" 
                         Background="Transparent" BorderThickness="0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignNavigationListBoxItem}">
                            <!-- No additional setters needed -->
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="{Binding Icon}" Width="24" Height="24" Margin="0,0,12,0" />
                                <TextBlock Text="{Binding Name}" FontSize="16" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <ContentControl Content="{Binding CurrentPage}" />
        </ScrollViewer>
    </Grid>
</Window>